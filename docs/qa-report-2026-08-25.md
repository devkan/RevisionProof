# QA evidence, 2026-08-25

Historical fixture-slice snapshot. For the deployed candidate-aware flow and current test counts, see [the 2026-08-26 Cloud deployment report](qa-report-2026-08-26-cloud-deploy.md).

## Result

Status: PASS for the fixture hackathon path. Health score: 100/100 after fixes. Design score: A-. AI slop score: A.

## Browser path

gstack browse ran against the production Docker image at `http://127.0.0.1:18080`:

1. Fixture mode and runtime truth were visible before any action.
2. Feedback interpretation returned explicit `fixture.interpreter` provenance.
3. Three time-coded evidence anchors returned explicit `fixture.segment_index` provenance.
4. A/B FFmpeg previews rendered; option B froze a SHA-256 `RevisionSpec`.
5. Demo v2 passed patch/audio checks and failed the locked CTA check, producing `PUBLISH BLOCKED`.
6. Demo v3 passed all three checks and produced `PUBLISH READY`.
7. Raw run JSON retained the full 11-event history, including both BLOCKED and READY transitions.

Console errors: 0. Broken network requests: 0. Initial load: 148 ms locally. Desktop horizontal overflow: 0.

## Responsive and design findings

The first mobile audit found two issues: the empty proof trace appeared before the primary task, and the brand/primary button were below the 44px touch target. Both were fixed and re-tested at 375x812:

- primary task top: 453px
- proof trace top: 838px
- horizontal overflow: 0px
- interactive controls below 44px: 0
- rendered fonts: bundled Manrope Variable and Newsreader Variable
- console errors: 0

Screenshot evidence is retained under `.gstack/qa-reports/screenshots/` and is intentionally excluded from Git.

## Automated and runtime evidence

- 28 backend tests pass.
- Ruff check and format check pass.
- ESLint and Vite production build pass.
- 12 real 30-second H.264/AAC regression candidates: 3 expected PASS, 9 expected FAIL, 0 false PASS.
- Three consecutive v1 to v2 BLOCKED to v3 READY rehearsals pass.
- Docker image builds, `/healthz` and `/readyz` pass, demo assets and SPA return HTTP 200.
- ClickHouse 25.6 schema initializes; the official `mcp-clickhouse` executable returns a row through `run_query`; direct base-table reads by the MCP reader and SELECT by the writer are denied.

## Deferred by design

Google ADK/Gemini, Vertex embedding, GCS, ClickHouse Cloud TLS, and Cloud Run deployment remain credential-unverified. Fixture traces must not be presented as live evidence.
